import { Router } from "express";

import { expressRouteAdapter } from "../adapters/express-route-adapter";

export const refreshTokenRoutes=Router();

// refreshTokenRoutes.post('/',expressRouteAdapter());